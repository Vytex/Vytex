from flask import Flask

from flaskext.mysql import MySQL

app = Flask(__name__)
mysql = MySQL()

# initialize the application
# MYSQL_DATABASE_HOST 	default is ‘localhost’
# MYSQL_DATABASE_PORT 	default is 3306
# MYSQL_DATABASE_USER 	default is None
# MYSQL_DATABASE_PASSWORD 	default is None
# MYSQL_DATABASE_DB 	default is None
# MYSQL_DATABASE_CHARSET 	default is ‘utf-8’'

# config
app.config['SECRET_KEY'] = 'alpacastonks'
app.config['MYSQL_DATABASE_HOST'] = 'localhost'
app.config['MYSQL_DATABASE_USER'] = 'yudhvir'
app.config['MYSQL_DATABASE_PASSWORD'] = r'fQ<7wUd[BV6+J+t<'
# app.config['MYSQL_DATABASE_DB'] = 'app'
app.config['MYSQL_DATABASE_PORT'] = 3306

mysql.init_app(app)
mysql.connect()

print(mysql.get_db())
cursor = mysql.get_db().cursor()

print("running the main func")

from app import routes

# from app import models
from app import controller